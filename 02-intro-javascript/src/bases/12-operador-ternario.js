// Operador Condicional Ternario

const activo = true;

// let mensaje;

// if ( activo ) {
//     mensaje = 'Activo';
// } else {
//     mensaje = 'Inactivo';
// }

// Esto es lo mismo que el codigo de arriba
const mensaje = ( activo ) ? 'Activo' : 'Inactivo';

// Si solo queremos validar el if y no el else
// const mensaje = ( activo ) && 'Activo';

console.log( mensaje );